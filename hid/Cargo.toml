[package]
name = "hid"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["fs", "time", "sync"] }
thiserror = "2.0"
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
